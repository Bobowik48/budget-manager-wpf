<Window x:Class="BudgetManager.Views.TransactionDetailWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Szczegóły transakcji"
        Height="420" Width="480"
        MinHeight="400" MinWidth="440"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="#F5F7FA">

    <Window.Resources>
        <Style x:Key="LabelStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="Foreground" Value="#6B7280"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Margin" Value="0,0,0,3"/>
        </Style>
        <Style x:Key="ValueStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="15"/>
            <Setter Property="Foreground" Value="#111827"/>
            <Setter Property="Margin" Value="0,0,0,16"/>
            <Setter Property="TextWrapping" Value="Wrap"/>
        </Style>
    </Window.Resources>

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Nagłówek -->
        <Border Grid.Row="0" Background="#1F2937" CornerRadius="12"
                Padding="20,16" Margin="0,0,0,20">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0">
                    <TextBlock Text="{Binding Description}"
                               FontSize="18" FontWeight="Bold" Foreground="White"
                               TextWrapping="Wrap"/>
                    <TextBlock Text="{Binding Date, StringFormat={}{0:dddd, dd MMMM yyyy}}"
                               FontSize="12" Foreground="#9CA3AF" Margin="0,4,0,0"/>
                </StackPanel>

                <Border Grid.Column="1" CornerRadius="20" Padding="12,6"
                        VerticalAlignment="Center" Margin="16,0,0,0">
                    <Border.Style>
                        <Style TargetType="Border">
                            <Setter Property="Background" Value="#B91C1C"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding Type}" Value="Przychód">
                                    <Setter Property="Background" Value="#15803D"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Border.Style>
                    <TextBlock Text="{Binding Type}"
                               FontSize="13" FontWeight="Bold" Foreground="White"/>
                </Border>
            </Grid>
        </Border>

        <!-- Szczegóły -->
        <Border Grid.Row="1" Background="White" CornerRadius="12"
                Padding="20" BorderBrush="#E5E7EB" BorderThickness="1">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- Kwota -->
                <StackPanel Grid.Row="0" Grid.ColumnSpan="2" Margin="0,0,0,8">
                    <TextBlock Text="KWOTA" Style="{StaticResource LabelStyle}"/>
                    <TextBlock Text="{Binding Amount, StringFormat={}{0:N2} zł}"
                               FontSize="32" FontWeight="Bold" Margin="0,0,0,16">
                        <TextBlock.Style>
                            <Style TargetType="TextBlock">
                                <Setter Property="Foreground" Value="#B91C1C"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding Type}" Value="Przychód">
                                        <Setter Property="Foreground" Value="#15803D"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </TextBlock.Style>
                    </TextBlock>
                </StackPanel>

                <!-- Kategoria / Data -->
                <StackPanel Grid.Row="1" Grid.Column="0">
                    <TextBlock Text="KATEGORIA" Style="{StaticResource LabelStyle}"/>
                    <TextBlock Text="{Binding Category}" Style="{StaticResource ValueStyle}"/>
                </StackPanel>
                <StackPanel Grid.Row="1" Grid.Column="1">
                    <TextBlock Text="DATA" Style="{StaticResource LabelStyle}"/>
                    <TextBlock Text="{Binding Date, StringFormat=dd.MM.yyyy}"
                               Style="{StaticResource ValueStyle}"/>
                </StackPanel>

                <!-- Separator -->
                <Border Grid.Row="2" Grid.ColumnSpan="2"
                        Height="1" Background="#E5E7EB" Margin="0,0,0,16"/>

                <!-- Opis -->
                <StackPanel Grid.Row="3" Grid.ColumnSpan="2">
                    <TextBlock Text="OPIS" Style="{StaticResource LabelStyle}"/>
                    <TextBlock Text="{Binding Description}"
                               FontSize="14" Foreground="#374151"
                               TextWrapping="Wrap" LineHeight="22"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Zamknij -->
        <Button Grid.Row="2" Content="Zamknij"
                Width="120" Height="38"
                HorizontalAlignment="Right"
                Margin="0,16,0,0"
                Click="CloseButton_Click"
                Background="#1F2937" Foreground="White"
                BorderBrush="#1F2937" FontSize="14"/>
    </Grid>
</Window>